node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Claude Code (machine-specific)
.claude/settings.local.json
# Agent git worktrees. eslint takes its ignore list from this file, so without
# this it walks the checked-in iOS build output inside a worktree and reports
# thousands of errors in minified chunks — breaking `npm run lint`, the ship gate.
.claude/worktrees/

# Design handoffs (reference material, not source — kept outside the repo)
design_handoff_*/
*_handoff.md

# Local design-review captures (scripts/design-review.mjs)
design-review/
design-review-before/
